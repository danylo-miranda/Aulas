tupla = 'a','b','c','d'

# verificar a posição
print(tupla.index('c'))

# verifica a quantidade de vezes que um elemento aparece na tupla
print(tupla.count('a'))

# verifica a quantidade de itens
print(len(tupla))